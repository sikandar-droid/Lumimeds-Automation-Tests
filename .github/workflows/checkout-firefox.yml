name: Checkout - Firefox

on:
  schedule:
    - cron: '30 8 * * *'  # 3:30 AM EST daily (8:30 AM UTC)
  workflow_dispatch:
    inputs:
      environment:
        description: 'Test environment'
        required: true
        default: 'production'
        type: choice
        options:
          - staging
          - production
      record_video:
        description: 'Record video of test'
        required: false
        default: true
        type: boolean

jobs:
  test:
    name: Firefox Checkout Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: 📦 Install dependencies
        run: npm ci
      
      - name: 💾 Cache Playwright browsers
        uses: actions/cache@v3
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-firefox-v2-${{ hashFiles('**/package-lock.json') }}
      
      - name: 🎭 Install Firefox
        run: npx playwright install firefox --with-deps
      
      - name: 🧪 Run Staging Checkout
        if: github.event.inputs.environment == 'staging'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          RECORD_VIDEO: ${{ github.event.inputs.record_video }}
          MASK_PAYMENT: 'true'
          TEST_FILE: 'Checkout-Firefox-Staging'
          TEST_ENV: 'Staging'
          TEST_URL: 'https://staging.lumimeds.com'
          TEST_BROWSER: 'Firefox'
        run: |
          npx playwright test tests/checkout/checkout.spec.js --project=firefox
      
      - name: 🚀 Run Production Checkout
        if: github.event.inputs.environment != 'staging'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          RECORD_VIDEO: ${{ github.event.inputs.record_video }}
          MASK_PAYMENT: 'true'
          TEST_FILE: 'Checkout-Firefox-Production'
          TEST_ENV: 'Production'
          TEST_URL: 'https://lumimeds.com'
          TEST_BROWSER: 'Firefox'
        run: |
          npx playwright test tests/checkout/checkout-prod.spec.js --project=firefox
      
      - name: 🎬 Upload Test Video
        if: success() && github.event.inputs.record_video == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: checkout-video-firefox-${{ github.run_number }}
          path: test-results/**/video.webm
          retention-days: 30
          if-no-files-found: ignore
      
      - name: 📤 Send Slack Notification (Staging)
        if: always() && github.event.inputs.environment == 'staging'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          TEST_FILE: 'Checkout-Firefox-Staging'
          TEST_ENV: 'Staging'
          TEST_URL: 'https://staging.lumimeds.com'
          TEST_BROWSER: 'Firefox'
          RECORD_VIDEO: ${{ github.event.inputs.record_video }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_RUN_NUMBER: ${{ github.run_number }}
        run: node scripts/slack-notify-checkout.js
      
      - name: 📤 Send Slack Notification (Production)
        if: always() && github.event.inputs.environment != 'staging'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          TEST_FILE: 'Checkout-Firefox-Production'
          TEST_ENV: 'Production'
          TEST_URL: 'https://lumimeds.com'
          TEST_BROWSER: 'Firefox'
          RECORD_VIDEO: ${{ github.event.inputs.record_video }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_RUN_NUMBER: ${{ github.run_number }}
        run: node scripts/slack-notify-checkout.js
      
      - name: 📊 Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: checkout-firefox-results
          path: test-results.json
          retention-days: 30
