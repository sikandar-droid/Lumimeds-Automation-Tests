name: Daily Checkout Test

on:
  # SCHEDULE DISABLED - Set your schedule here when ready
  # Examples:
  # - cron: '30 11 * * *'     # Daily at 3:30 AM PST
  # - cron: '0 9 * * 1-5'     # Weekdays at 9 AM
  # - cron: '0 */6 * * *'     # Every 6 hours
  
  # Manual trigger from GitHub UI (always available)
  workflow_dispatch:
    inputs:
      environment:
        description: 'Test environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production

jobs:
  checkout-test:
    name: Run Checkout Test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: 📦 Install dependencies
        run: npm ci
      
      - name: 💾 Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v3
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-
      
      - name: 🎭 Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install chromium --with-deps
      
      - name: 🎭 Install Playwright dependencies
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps chromium
      
      - name: 🧪 Run Staging Checkout Test
        if: github.event.inputs.environment != 'production'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          TEST_FILE: 'checkout'
          TEST_ENV: 'Staging'
          TEST_URL: 'https://staging.lumimeds.com'
        run: |
          npx playwright test tests/checkout/checkout.spec.js --project=chromium
          node scripts/slack-notify-checkout.js
      
      - name: 🚀 Run Production Checkout Test
        if: github.event.inputs.environment == 'production'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          TEST_FILE: 'checkout-prod'
          TEST_ENV: 'Production'
          TEST_URL: 'https://lumimeds.com'
        run: |
          npx playwright test tests/checkout/checkout-prod.spec.js --project=chromium
          node scripts/slack-notify-checkout.js
      
      - name: 📊 Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results.json
          retention-days: 30
      
      - name: 📸 Upload screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots
          path: screenshots/
          retention-days: 7
      
      - name: 📄 Upload HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30


